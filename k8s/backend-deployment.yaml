apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: fortune-cookie
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: mpldocker/backend:latest
        ports:
        - containerPort: 8080
